{
  "name": "enzyme-context-patch",
  "version": "0.0.7",
  "main": "index.js",
  "license": "MIT",
  "repository": "KevinGrandon/enzyme-react-16-context-patch",
  "bin": "bin/patch-enzyme.sh",
  "scripts": {
    "postinstall": "echo 'Patching from postinstall.' && pwd && ls -la && (cd bin && ls -la) && \"$(pwd)/bin/patch-enzyme.sh\""
  },
  "peerDependencies": {
    "enzyme": "^3.3.0"
  },
  "dependencies": {}
}
